// Package brand defines Skpr brand assets used in pinchy's terminal output.
package brand

// Accent is the Skpr brand accent color used to highlight section headings in
// pinchy's terminal output. It is a vivid violet (Charple) chosen to harmonise
// with fang's default palette — sitting naturally next to Malibu program names,
// Pony command names, and Coral quoted strings without competing for attention.
// Pass as a lipgloss.Color: lipgloss.Color(brand.Accent).
const Accent = "#6B50FF"
